import type { ComponentPropsWithoutRef } from "react";

/**
 * Props for the {@link Toolbox} organism. Renders a `<section>` whose children
 * are SkillCategory cards laid out in a responsive grid.
 */
export type ToolboxProps = ComponentPropsWithoutRef<"section">;
