import type { ComponentPropsWithoutRef } from "react";

/**
 * Props for the {@link OpenSource} organism. Renders a `<section>`; children are
 * RepoRow items shown inside one bordered panel.
 */
export type OpenSourceProps = ComponentPropsWithoutRef<"section">;
