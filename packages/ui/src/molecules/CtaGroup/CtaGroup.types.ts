import type { ComponentPropsWithoutRef } from "react";

/**
 * Props for the {@link CtaGroup} molecule. A layout wrapper that lays out a
 * pair of call-to-action controls (e.g. a primary Button + a ghost Button).
 */
export type CtaGroupProps = ComponentPropsWithoutRef<"div">;
