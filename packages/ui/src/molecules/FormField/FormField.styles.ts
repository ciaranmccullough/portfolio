export const formFieldLabelClass = "block";

export const formFieldCaptionClass =
  "mb-2 block font-mono text-2xs font-bold uppercase tracking-wide text-fg-faint";

/** Applied to the control when the field is invalid. */
export const formFieldControlInvalidClass = "border-brand-orange";

/**
 * The error message slot. `min-h-4` reserves one line so the layout stays put
 * whether or not a message is showing (no content jump).
 */
export const formFieldErrorClass =
  "mt-1.5 block min-h-4 font-mono text-2xs font-semibold text-brand-orange";
