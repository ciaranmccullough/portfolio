// Stub for CSS imports under Jest — returns an empty object so `import "./x.css"`
// resolves harmlessly. (@portfolio/ui uses Tailwind utility classes, not CSS
// modules, so this is a safety net rather than a routine need.)
module.exports = {};
