echo "▶ pre-push: lint · typecheck · format · build — push is blocked if any step fails"
set -e

pnpm run lint
pnpm run typecheck
pnpm run format
pnpm run build

echo "✓ pre-push checks passed — pushing"
