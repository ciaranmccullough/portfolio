echo "▶ pre-push: lint · format · build · typecheck · test — push is blocked if any step fails"
set -e

pnpm run lint
pnpm run format
pnpm run build
pnpm run typecheck
pnpm run test

echo "✓ pre-push checks passed — pushing"
