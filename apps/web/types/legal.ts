import type { Document } from "@contentful/rich-text-types";

/**
 * A legal page's body content as the UI consumes it — a Contentful Rich Text
 * document (rendered by the `RichText` component).
 */
export type LegalDocument = Document;
