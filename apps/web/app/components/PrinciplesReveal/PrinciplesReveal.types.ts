import type { PrinciplesProps } from "@portfolio/ui";

/** Props for {@link PrinciplesReveal} — identical shape to the presentational
 *  `Principles` organism it recomposes. */
export type PrinciplesRevealProps = PrinciplesProps;
