import type { WalkthroughProps } from "@portfolio/ui";

/** Props for {@link StoryWalkthrough} — identical shape to the presentational
 *  `Walkthrough` organism it wraps (and falls back to verbatim). */
export type StoryWalkthroughProps = WalkthroughProps;
