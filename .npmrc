# The user's home-directory package.json declares yarn as packageManager.
# This workspace is pnpm; disable the strict cross-tree check so pnpm runs here.
package-manager-strict=false
package-manager-strict-version=false
